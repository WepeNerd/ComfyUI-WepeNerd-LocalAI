# Prompt enhancement

Open [prompt-enhancement.json](prompt-enhancement.json), select your installed
GGUF in Local AI Model, enter a prompt, and click **Queue**. Preview Any displays
the enhanced text. The example does not download a model; llama-server must be
installed separately. A projector is only required when supplying image inputs.
